{
  "name": "really-bad-design-museum",
  "version": "1.0.0",
  "private": true,
  "description": "A lovingly curated collection of terrible website ideas.",
  "scripts": {
    "start": "node build.js && node server.js",
    "build": "node build.js",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev",
    "check": "node --check app.js && node --check additional-exhibits.js && node --check arcade-exhibits.js && node --check puzzle-exhibits.js && node --check server.js && node --check build.js && node --check worker.mjs"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  }
}
